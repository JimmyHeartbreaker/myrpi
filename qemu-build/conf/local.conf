MACHINE ?= "qemuarm64"
DISTRO_FEATURES:remove = "x11 wayland"
DISTRO_FEATURES:append = " systemd dhcp bluez5 bluetooth wifi libsdl2 directfb debuginfod"
VIRTUAL_RUNTIME_init_manager = "systemd"
IMAGE_INSTALL:append = " bluez5 python3-smbus bridge-utils gdbserver hostapd iptables wpa-supplicant libsdl2 libsdl-image helloworld helloworld-asm"
EXTRA_IMAGE_FEATURES:append = " tools-debug debug-tweaks ssh-server-dropbear package-management"
CORE_IMAGE_EXTRA_INSTALL += "apt dhcpcd dhcp-relay"
ENABLE_I2C = "1"
ENABLE_UART = "1"
IMAGE_GEN_DEBUGFS = "1"
IMAGE_FSTYPES_DEBUGFS = "tar.bz2"
RPI_EXTRA_CONFIG = ' \n \
    dtoverlay=miniuart-bt \n \
    '
